import { ConnectionOptions } from 'typeorm';
import { ModuleWithProviders } from '@angular/core';
export declare class AngularTypeormModule {
    /**
     * @param duration time in seconds
     * @description Pause for given amount of time
     */
    private static wait;
    /**
     * @param configs  connection options
     * @description Initialize TypeOrm connection for root
     */
    static forRoot(configs: ConnectionOptions): ModuleWithProviders<AngularTypeormModule>;
}
